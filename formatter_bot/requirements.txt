# 🤖 Formatter Bot - المتطلبات الأساسية
# متوافق مع Python 3.12 و Pillow 11.0.0

python-telegram-bot==20.7
Pillow==11.0.0
pillow-heif==0.18.0
opencv-python-headless==4.9.0.80
requests==2.31.0




















# 🤖 Formatter Bot - المتطلبات الأساسية
# =====================================

# ---------- بوت التيليجرام ----------
# python-telegram-bot==20.7          # مكتبة التيليجرام الرسمية

# ---------- معالجة الصور ----------
# Pillow==10.1.0                      # معالجة الصور الأساسية
# pillow-heif==0.15.0                  # دعم صيغة HEIC (صور الآيفون)
# opencv-python-headless==4.8.1.78     # معالجة متقدمة للصور والفيديو (نسخة خفيفة)

# ---------- طلبات HTTP والتواصل ----------
# requests==2.31.0                     # التواصل مع APIs الخارجية

# ---------- ذكاء اصطناعي (اختياري) ----------
# realesrgan                         # لتحسين الصور بجودة عالية (يلزم تثبيت منفصل)
# torch                              # للمكتبات التي تحتاج PyTorch

# ---------- أدوات تطوير (اختياري) ----------
# black                              # تنسيق الكود
# pylint                             # فحص الكود



# python-telegram-bot
# requests
# Pillow
# pillow-heif
# opencv-python-headless

